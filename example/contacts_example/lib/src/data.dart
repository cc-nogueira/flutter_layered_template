/// Data Layer exports:
///
/// Data layer only needs to expose its layer object.
/// It will be accessed by main.dart to:
///   - be initialized.
///   - get data layer provisions and hand them to the DomainLayer.

export 'data/layer/data_layer.dart';
