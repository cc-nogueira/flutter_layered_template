/// Service Layer exports.
///
/// Service layer only needs to expose its layer object.
///
/// This layer object will be accessed by main.dart to:
///   - be initialized.
///   - provision the DomainLayer with service implementations.

export 'service/layer/service_layer.dart';
